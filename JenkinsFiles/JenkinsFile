pipeline {
    agent any

    stages {

        stage('Build') {
            tools {
                jdk
            }
            steps {
                script {
                    sh "chmod +x ./gradlew"
                    sh "./gradlew clean build -p webFlux"
                }
            }
        }
    }

}
